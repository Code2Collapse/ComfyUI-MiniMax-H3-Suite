# Moved

This worklog is now `WORKLOG_ComfyUI-MiniMaxSuite.md`, matching the
`WORKLOG_<repo>.md` convention used by the other five packs. Renamed 2026-08-29
because the odd name made it invisible to tooling that globs `WORKLOG_*`.
